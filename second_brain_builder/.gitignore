# filename: second_brain_builder/.gitignore
# purpose: Updated to ensure logs/ and vault/ are never committed (but still created by run.sh)

# Runtime folders (never commit)
logs/
vault/
vector_index/

# Standard ignores
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.DS_Store
*.db
*.log
